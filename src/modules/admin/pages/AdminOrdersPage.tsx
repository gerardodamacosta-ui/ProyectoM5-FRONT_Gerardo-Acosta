// placeholder temporal — el listado/filtro real se suma en un prompt posterior
export function AdminOrdersPage() {
  return <h2 className="text-xl font-semibold">Gestión de órdenes (en construcción)</h2>
}

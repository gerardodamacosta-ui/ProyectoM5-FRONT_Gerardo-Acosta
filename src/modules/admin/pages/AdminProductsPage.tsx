// placeholder temporal — el CRUD real se suma en el siguiente prompt de este hito
export function AdminProductsPage() {
  return <h2 className="text-xl font-semibold">Gestión de productos (en construcción)</h2>
}
